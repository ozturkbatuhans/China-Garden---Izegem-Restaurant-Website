<div class="container-centre">
    <section class="form-sent">
        <div class="container">
            <img src="{{img}}">
            <h3>{{title}}</h3>
            <p>{{message}}</p>
            <a href="/contact"><button class="button">Terug</button></a>
        </div>
</div>
</section>